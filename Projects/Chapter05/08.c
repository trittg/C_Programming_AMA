#include <stdio.h>
/**
 * Name:    08.c
 * Date:    2018 07 05
 * Author:  trittg
 * Project: 5.8 Pg. 96
 * Purpose: Asks a user to enter a time (24 hour format), program then displays
 *          the arrival and departure times for the flight whose departure time
 *          is closest to the input time.
 */

int main(void)
{
    int hours = 0, minutes = 0;

    printf("Enter the the time in 24 hour format (00:00): ");
    scanf ("%d:%d", &hours, &minutes);

    //just going to skip this project because of how tedious it is

    return 0;
}